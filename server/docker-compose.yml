version: "2"
services:
  app:
    container_name: app
    restart: always
    build: .
    ports:
      - "80:3000"
    links:
      - iro-mongo
    depends_on:
      - iro-mongo
  iro-mongo:
    container_name: mongo
    image: mongo
    volumes:
      - /var/data:/data/db
